generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  idU          Int            @id @default(autoincrement())
  userName     String         @unique
  password     String         
}

